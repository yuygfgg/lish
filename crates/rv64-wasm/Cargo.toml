[package]
name = "rv64-wasm"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Wasm runtime surface for the Lish full-system VM"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
rv64-core = { path = "../rv64-core" }
rv64-system = { path = "../rv64-system" }
rv64-jit = { path = "../rv64-jit" }
