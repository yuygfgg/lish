[package]
name = "rv64-system"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "RISC-V virt full-system machine for Lish"
publish = false

[dependencies]
rv64-core = { path = "../rv64-core" }

[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
libc = "=0.2.189"
